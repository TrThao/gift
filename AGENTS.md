# Hướng dẫn cho AI / agent

Trang quà tương tác một lần (Ánh, 16/08, mật mã `1608`).

## Đọc trước

**[FLOW.md](./FLOW.md)**

## Luồng

`waiting` → `prelude` → `letter` → `memory` → cinematic → `unlock` → `gift` → **finale (END)**

Mở quà = lời chúc → *Happy Birthday, Ánh ❤️* → fade to black. Không trang sau.

## Tone text

- Ngắn gọn, tự nhiên, sinh nhật
- Không áp lực, không quá tỏ tình

## Không sửa khi chỉ đổi text

- `giftCinematic`, SVG vẽ tay, animation manWalk/bouquet/handoff
- `PASSWORD`, `DEFAULT_MONTH`, `DEFAULT_DAY`, `RECIPIENT_NAME` (trừ khi user yêu cầu)
